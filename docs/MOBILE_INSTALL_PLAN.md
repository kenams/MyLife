# Mobile installability workstream

Goal: make the existing responsive web build feel first-class on a phone without creating a second game.

Planned checks: web manifest/app metadata, home-screen launch behavior, standalone-safe viewport/layout, auth persistence, map-first entry, and phone smoke coverage.
